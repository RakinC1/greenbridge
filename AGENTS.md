# AGENTS.md

## Cursor Cloud specific instructions

This repository (`greenbridge`) is currently empty — it contains only a `README.md`. There are no applications, services, dependencies, build systems, or tests to run.

When application code is added in the future, update this section with:
- How to install dependencies and which package manager to use
- How to run the dev server, lint, tests, and build
- Any non-obvious startup caveats or environment requirements
